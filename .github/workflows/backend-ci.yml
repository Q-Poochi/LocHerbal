name: Backend CI

on:
  push:
    branches: [main, develop]
    paths: ['LocProject/**']
  pull_request:
    branches: [main]
    paths: ['LocProject/**']

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres_password
          POSTGRES_DB: ecommerce_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: LocProject/package-lock.json

      - name: Install dependencies
        working-directory: LocProject
        run: npm ci

      - name: Setup test environment
        working-directory: LocProject
        run: |
          cp .env.example .env
          echo "DATABASE_URL=postgresql://postgres:postgres_password@localhost:5432/ecommerce_test" >> .env
          echo "DIRECT_URL=postgresql://postgres:postgres_password@localhost:5432/ecommerce_test" >> .env
          echo "JWT_ACCESS_SECRET=ci_access_secret_not_for_production" >> .env
          echo "JWT_REFRESH_SECRET=ci_refresh_secret_not_for_production" >> .env
          echo "REDIS_HOST=localhost" >> .env
          echo "REDIS_PORT=6379" >> .env

      - name: Run migrations
        working-directory: LocProject
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: postgresql://postgres:postgres_password@localhost:5432/ecommerce_test
          DIRECT_URL: postgresql://postgres:postgres_password@localhost:5432/ecommerce_test

      - name: Run tests
        working-directory: LocProject
        run: npm test -- --forceExit
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://postgres:postgres_password@localhost:5432/ecommerce_test
          DIRECT_URL: postgresql://postgres:postgres_password@localhost:5432/ecommerce_test
          JWT_ACCESS_SECRET: ci_access_secret_not_for_production
          JWT_REFRESH_SECRET: ci_refresh_secret_not_for_production
          REDIS_HOST: localhost
          REDIS_PORT: 6379

      - name: Run e2e saga tests
        working-directory: LocProject
        run: npx jest --config test/jest-e2e.json --forceExit
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://postgres:postgres_password@localhost:5432/ecommerce_test
          DIRECT_URL: postgresql://postgres:postgres_password@localhost:5432/ecommerce_test
          JWT_ACCESS_SECRET: ci_access_secret_not_for_production
          JWT_REFRESH_SECRET: ci_refresh_secret_not_for_production
          VNP_TMN_CODE: CI_TEST
          VNP_HASH_SECRET: ci_vnp_hash_not_for_production
          VNP_URL: https://sandbox.vnpayment.vn/
          REDIS_HOST: localhost
          REDIS_PORT: 6379

      - name: Build
        working-directory: LocProject
        run: npm run build